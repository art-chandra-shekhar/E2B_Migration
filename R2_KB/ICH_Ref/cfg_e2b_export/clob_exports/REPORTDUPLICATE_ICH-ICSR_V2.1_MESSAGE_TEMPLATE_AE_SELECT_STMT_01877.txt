SELECT
  CASE
    WHEN INSTR(REF_NO,'-',1,2) > 0
    THEN SUBSTR(SUBSTR(REF_NO,4,(INSTR(REF_NO,'-',1,2)-4)),1,50)
    ELSE SUBSTR(NOTES,1,50)
  END DUPLICATESOURCE,
  REF_NO DUPLICATENUMB
FROM CASE_REFERENCE
WHERE CASE_ID   = :CASE_ID
AND REF_TYPE_ID = 12
AND REF_NO     IS NOT NULL