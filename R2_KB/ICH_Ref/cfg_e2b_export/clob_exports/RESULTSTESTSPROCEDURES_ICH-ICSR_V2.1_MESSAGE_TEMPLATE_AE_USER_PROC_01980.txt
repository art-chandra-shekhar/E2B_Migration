DECLARE
   v_xml      varchar2(32767);
   l_return   number := 0;
BEGIN
   v_xml  := ESM_IMP.F_READ(:REPORT_ID,'RESULTSTESTSPROCEDURES');
   IF upper(v_xml) is not null then
      l_return :=  ESM_IMP.F_WRITE(:REPORT_ID,:PARENT_ELEMENT,:DTD_ELEMENT,:PROFILE,'CASE_PAT_TESTS','REL_TESTS_J',v_xml);
   END IF;
END;