DECLARE
  v_xml1			varchar2(32767);
  l_return			number := 0;
BEGIN
	v_xml1    := trim(ESM_IMP.F_READ_EXTENSION(:REPORT_ID,:DTD_ELEMENT));
	if v_xml1 is not null then
		l_return :=  ESM_IMP.F_WRITE(:REPORT_ID,:PARENT_ELEMENT,:DTD_ELEMENT,:PROFILE,'CASE_EVENT','SC_OTHER_TEXT',v_xml1);
	end if;
END;